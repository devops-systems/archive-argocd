apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sync-apps-features
  namespace: argocd
  labels:
    name: app.kubernetes.io/instance=sync-apps-features
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/devops-systems/argocd-gitops.git
    targetRevision: main
    path: apps-features
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
